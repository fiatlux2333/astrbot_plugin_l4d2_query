# AstrBot L4D2 Query Plugin 依赖
#
# 安装 Python 依赖：
#   pip install -r requirements.txt
#
# 安装 Playwright 浏览器（图片渲染必需）：
#   playwright install chromium
#
# 安装中文字体（Linux 服务器图片渲染中文不乱码必需）：
#   Debian/Ubuntu:  apt install -y fonts-noto-cjk
#   RHEL/CentOS:    yum install -y google-noto-cjk-fonts
#   Alpine:         apk add font-noto-cjk
#   macOS/Windows:  系统自带中文字体，无需额外安装
python-a2s>=1.3.0
aiohttp>=3.9
aiomysql>=0.2.0
jinja2>=3.1
playwright>=1.40
rcon>=2.0
